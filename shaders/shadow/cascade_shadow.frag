#version 450

// Fragment shader for shadow map generation
// Outputs nothing - we only care about depth

void main() {
    // Optional: alpha testing for vegetation
    // if (alpha < 0.5) discard;
}
